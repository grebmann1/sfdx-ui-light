# Dependencies
node_modules
apps/ui/node_modules
apps/docs/node_modules
packages/vscode/node_modules
packages/desktop/node_modules

# Intermediate / dev-only build outputs (not needed in Docker context)
site
__lwr_cache__
packages/shared/dist

# Keep dist/ and packages/*/dist — they are pre-built locally before docker build

# Git
.git
.gitignore

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Environment files (injected at runtime)
.env
.env.*

# Editor / OS
.DS_Store
.cursor
*.log
npm-debug.log*

# Desktop app (not needed in server Docker image)
packages/desktop

# Test artifacts
coverage
*.test.js.map
